apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-deforestation-reporting-service.fullname" . }}
  labels:
    {{- include "cf-deforestation-reporting-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cf-deforestation-reporting-service.selectorLabels" . | nindent 4 }}
